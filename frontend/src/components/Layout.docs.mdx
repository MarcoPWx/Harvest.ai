import { Meta, ArgsTable, Canvas, Story } from "@storybook/addon-docs";
import Layout from "./Layout";

<Meta title="Components/Layout/Docs" component={Layout} />

<ArgsTable of={Layout} />

## Examples

<Canvas>
  <Story name="Home Page">
    <Layout currentPage="home">
      <div style={{ padding: "2rem", minHeight: "50vh" }}>
        <h1 style={{ fontSize: "2rem", fontWeight: 700 }}>Welcome to Harvest.ai</h1>
        <p style={{ marginTop: "0.5rem" }}>
          This page demonstrates the Layout wrapper with the Home tab active.
        </p>
      </div>
    </Layout>
  </Story>
</Canvas>

<Canvas>
  <Story name="Demo Page">
    <Layout currentPage="demo">
      <div style={{ padding: "2rem", minHeight: "50vh" }}>
        <h1 style={{ fontSize: "2rem", fontWeight: 700 }}>Demo</h1>
        <p style={{ marginTop: "0.5rem" }}>Try content generation and streaming UX here.</p>
      </div>
    </Layout>
  </Story>
</Canvas>

## Accessibility

- Ensure the page content includes a single H1 (`<h1>`) as the primary title.
- Verify keyboard navigation order: header → main content → footer.
- If adding modals or overlays within pages, manage focus trapping and aria attributes.
